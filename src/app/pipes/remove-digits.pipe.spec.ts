import { RemoveDigitsPipe } from './remove-digits.pipe';

describe('RemoveDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
